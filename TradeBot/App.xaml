<Application x:Class="TradeBot.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:TradeBot"
             StartupUri="MainWindow.xaml">
    <Application.Resources>
        <ResourceDictionary>
            <Brush x:Key="BackgroundColor">#FFFFFF</Brush>
            <Brush x:Key="ForegroundColor">#2F2F2F</Brush>
            <Brush x:Key="ForegroundDarkerColor">#1A1A1A</Brush>
            <Brush x:Key="MainColor">#B378FF</Brush>
            <Brush x:Key="MainDarkerColor">#835CB6</Brush>
            <Brush x:Key="IntermediateColor">#F1F5FA</Brush>
            <Brush x:Key="IntermediateDarkerColor">#E4E8ED</Brush>
            
            <Style x:Key="Title" TargetType="TextBlock">
                <Setter Property="TextAlignment" Value="Left"></Setter>
                <Setter Property="VerticalAlignment" Value="Center"></Setter>
                <Setter Property="Foreground" Value="{StaticResource ForegroundColor}"></Setter>
                <Setter Property="FontSize" Value="24" />
                <Setter Property="FontWeight" Value="Bold" />
            </Style>
            
            <Style x:Key="ErrorText" TargetType="TextBlock">
                <Setter Property="Opacity" Value="0.6"></Setter>
                <Setter Property="Margin" Value="5 0 0 0"></Setter>
                <Setter Property="VerticalAlignment" Value="Center"></Setter>
                <Setter Property="FontSize" Value="13" />
                <Setter Property="Foreground" Value="Red" />
            </Style>
            
            <Style x:Key="LabelText" TargetType="TextBlock">
                <Setter Property="Opacity" Value="0.6"></Setter>
                <Setter Property="VerticalAlignment" Value="Center"></Setter>
                <Setter Property="FontSize" Value="13" />
            </Style>
            
            <Style x:Key="PrimaryButton" TargetType="{x:Type Button}">
                <Setter Property="Background" Value="{StaticResource MainColor}" />
                <Setter Property="Foreground" Value="{StaticResource BackgroundColor}" />
                <Setter Property="FontSize" Value="12" />
                <Setter Property="MinWidth" Value="128" />
                <Setter Property="Height" Value="28"></Setter>
                <Setter Property="SnapsToDevicePixels" Value="True" />

                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="{x:Type Button}">
                            <Border CornerRadius="8" Background="{TemplateBinding Background}">
                                <TextBlock TextAlignment="Center"
                                           VerticalAlignment="Center" HorizontalAlignment="Center"
                                           Padding="8, 0"
                                           Foreground="{TemplateBinding Foreground}"
                                           FontSize="{TemplateBinding FontSize}">
                                    <ContentPresenter Content="{TemplateBinding Content}"/>
                                </TextBlock>
                            </Border>

                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="{StaticResource MainDarkerColor}"></Setter>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>
            
            <Style x:Key="SecondaryButton" TargetType="{x:Type Button}">
                <Setter Property="Background" Value="{StaticResource IntermediateColor}" />
                <Setter Property="Foreground" Value="{StaticResource ForegroundColor}" />
                <Setter Property="FontSize" Value="12" />
                <Setter Property="MinWidth" Value="128" />
                <Setter Property="Height" Value="28"></Setter>
                <Setter Property="SnapsToDevicePixels" Value="True" />

                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="{x:Type Button}">
                            <Border CornerRadius="8" Background="{TemplateBinding Background}">
                                <TextBlock TextAlignment="Center"
                                           VerticalAlignment="Center" HorizontalAlignment="Center"
                                           Padding="8, 0"
                                           Foreground="{TemplateBinding Foreground}"
                                           FontSize="{TemplateBinding FontSize}">
                                    <ContentPresenter Content="{TemplateBinding Content}"/>
                                </TextBlock>
                            </Border>

                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="{StaticResource IntermediateDarkerColor}"></Setter>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>
        </ResourceDictionary>
    </Application.Resources>
</Application>